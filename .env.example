# A Countdown Timer — Environment Configuration
# Copy this file to .env and fill in your actual values

# DATABASE_URL
# PostgreSQL connection string for the timer application.
# Format: postgresql://username:password@host:port/database
# Example: postgresql://timer_user:password123@localhost:5432/countdown_timer
# For local development with default PostgreSQL on localhost:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/countdown_timer

# CORS_ORIGINS
# Comma-separated list of allowed origins for CORS (Cross-Origin Resource Sharing).
# This controls which frontend domains can make requests to this API.
# Use 'http://localhost:3000' for local React development (Vite default port).
# For production, list the actual frontend domain(s).
# Example: https://example.com,https://www.example.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# DEBUG (optional)
# Set to 'true' for verbose logging and FastAPI debug mode.
# Should be 'false' in production.
# DEBUG=false
