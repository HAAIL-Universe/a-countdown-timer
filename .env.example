# Database Configuration
# PostgreSQL connection string for timer application
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# Example: postgresql://postgres:password@localhost:5432/timer_db
DATABASE_URL=postgresql://postgres:password@localhost:5432/timer_db

# CORS Configuration
# Comma-separated list of allowed origins for Cross-Origin Resource Sharing
# Examples: http://localhost:5173,http://localhost:3000,https://example.com
# For development, typically http://localhost:5173 (Vite frontend)
# For production, specify exact domain(s) without trailing slashes
CORS_ORIGINS=http://localhost:5173,http://localhost:8000

# Application Settings (optional, add as needed)
# LOG_LEVEL=INFO
# API_TIMEOUT=30
